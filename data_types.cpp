#include <iostream> //biblioteca padrao

int main() { //função principal, aonde a magia acontece
    
    int x = 100;
    int y = 200;

    std::cout << "Hello! you sleep for "; //print em C++ (endl para finalizar a linha, caso ao contrario a mensagem exibira colada com a proxima)
    std::cout << x + y;
    std::cout << " years";

    // tipo de dados
    int a;
    float b;
    double A;
    int32_t c;
    bool d;
    char f;
    std::string e;

    // variavel imutavel (const)
    const float pi = 3.14;

    
    return 0;
}